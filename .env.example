# Copy to .env and fill in. Never commit real API keys.

# Semantic similarity floor (0–1). Above this, claim confidence gets a boost when embeddings align with evidence.
# Lower values = more trust in semantic similarity vs pure keyword overlap.
SIMILARITY_THRESHOLD=0.25

# Ollama (default local)
# OLLAMA_HOST=http://127.0.0.1:11434

